## Short Written Explanation
### Program Features:

#### This program is structured using multiple functions for modularity. 
#### It includes the reusable function calculatePercentage() for calculating X% of any number Y. 
#### The recursive function recursivePower() demonstrates recursion by calling itself to compute exponents.
#### Advantages of Recursion: Clean, elegant code that directly mirrors mathematical definitions.
#### Limitations of Recursion: Uses more stack memory, risks stack overflow, and is slower than loops.
